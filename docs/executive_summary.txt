Haan, agar ~100 files mein ~110 "use client" occurrences hain, toh definitely investigate karna worth it hai. But iska matlab 110 files = 110 performance problems nahi hai.

Actually, tumhare Bonzer audit mein jo 92 client files identify hue the, wahi concern tha. Dashboard refactor ne ek major unnecessary boundary hata diya, but likely aur bhi opportunities hain.

Main distinction

Agar 100 files mein "use client" hai:

100 Client Components
       ↓
Not necessarily bad
       ↓
Question:
"Kitne genuinely client hone chahiye?"

Example:

"use client"

function Button() {
  return <button onClick={...}>Save</button>
}

✅ Correct — interaction hai.

But:

"use client"

function PageHeader() {
  return (
    <div>
      <h1>Customers</h1>
      <a href="/customers">Back</a>
    </div>
  )
}

❌ Unnecessary — koi browser-side state/effect/event nahi.

Aur exactly aisa hi humne Dashboard ke PageHeader mein find kiya aur "use client" hata diya.

110 occurrences ko kaise tackle karenge?

Ek saath 110 files modify nahi karenge. 😄

Hum audit karenge aur categorize karenge:

Category	Action
Genuine interaction (onClick, state, effect, browser API)	🟢 Keep
Recharts / complex visualization	🟢 Keep
Form requiring client state	🟢 Usually keep
Pure rendering / static UI	🔴 Remove
Client only because parent was client	🔴 Investigate
Data fetching via useEffect	🟠 Convert to server architecture
Could be server but uncertain	🟡 Audit first
Aur ek VERY important point

"use client" transitive nahi hai upward.

Suppose:

Dashboard (Server)
   │
   ├── KpiCard (Client)
   │
   ├── Chart (Client)
   │
   └── PageHeader (Server)

This is perfectly valid.

We don't need to make the entire Dashboard client-side just because one child needs client JavaScript.

That's the architecture we're moving toward.

Tumhare case mein 110 occurrences actually ek signal hai

Especially because our audit already found:

Dashboard unnecessarily client
PageHeader unnecessarily client
multiple pages doing client-side Supabase fetching
large client component trees
Recharts
AppShell client
AuthProvider at root

So yes, I think we should eventually audit the entire "use client" footprint.

But I would not make that the next implementation task blindly.

We're currently at:

P0-F.1 Dashboard
       ✅ DONE

P0-F.2a Bundle measurement
       ⏭️ NEXT

Then:
       ↓
Identify highest-impact client boundaries
       ↓
F.2b / F.3 / F.4...

The bundle measurement will tell us whether the ~100 Client Components are actually translating into a meaningful JS/hydration cost.

So 110 occurrences = strong reason to investigate, not a reason to delete 110 lines.

And honestly, if we find that even 20–30 of those 100 files are unnecessarily Client Components, that could be a pretty meaningful performance improvement across the ERP.

ok Bundle measurement  

Haan. Ab P0-F.2a = Bundle Measurement only.
Is stage par kuch optimize nahi karna — pehle actual numbers nikalenge.

Nemotron ko ye prompt do:

BONZER ERP — P0-F.2a
CLIENT BUNDLE & JAVASCRIPT MEASUREMENT — READ-ONLY

P0-A through P0-F.1 are complete.

Current status:

P0-A — Auth Context Deduplication
DONE — React.cache() request-scoped deduplication

P0-B — Middleware
VERIFIED — no safe optimization required

P0-C — RLS Performance
DONE — static audit
LIKELY bottleneck
Runtime EXPLAIN ANALYZE unavailable

P0-D — permissions(name) Index
DONE — migration 024 created

P0-E — Reference Data Caching
DONE — existing cacheable reference data already uses appropriate
unstable_cache; no additional caching justified

P0-F.1 — Dashboard Structural Conversion
DONE
- dashboard/page.tsx is now a Server Component
- browser-side activity_log fetching removed
- ActivityFeed is Server Component
- PageHeader is Server Component
- loading.tsx added
- Recharts intentionally NOT optimized yet
- lint/typecheck/build PASS

==================================================
TASK
==================================================

Perform a READ-ONLY performance measurement of the Bonzer ERP's
client-side JavaScript and bundle composition.

DO NOT modify the application.

DO NOT optimize anything.

DO NOT remove "use client".

DO NOT lazy-load components.

DO NOT modify Recharts.

DO NOT modify package dependencies.

DO NOT modify Next.js configuration unless a temporary measurement-only
configuration is absolutely required; if so, do not commit it and report
it explicitly.

DO NOT modify AppShell/AuthProvider.

==================================================
PRIMARY OBJECTIVE
==================================================

We currently have approximately 100+ "use client" occurrences/files.

We need to determine whether this actually produces a significant
client JavaScript/hydration/bundle problem and which routes/components
are responsible.

Do NOT assume that a high count of "use client" automatically means
poor performance.

Measure the actual output.

==================================================
1. BUILD BASELINE
==================================================

Run:

npm run lint
npm run typecheck
npm run build

Confirm the build is clean before measurements.

Record:

- Next.js version
- build mode
- route list
- route sizes if Next reports them
- First Load JS where available
- shared JS size where available

==================================================
2. ANALYZE BUILD OUTPUT
==================================================

Inspect the production build output.

Determine:

- largest client JS chunks
- shared chunks
- route-specific chunks
- Recharts-related chunks
- Radix/shadcn-related chunks
- Supabase/browser client chunks
- other unexpectedly large dependencies

Identify the top 10 largest relevant JavaScript chunks.

For each provide:

Chunk | Approx Size | Main Dependencies/Purpose | Routes Using It

Do not guess dependency attribution if it cannot be established.

==================================================
3. DASHBOARD MEASUREMENT
==================================================

Specifically measure:

/dashboard

Compare the current architecture against the previous known state where
Dashboard was a monolithic Client Component.

We may not have an old build artifact.

If no historical artifact exists, DO NOT invent before/after numbers.

Instead report the current measurable state and identify what changed
architecturally.

Determine:

- dashboard-specific JS chunks
- Recharts contribution
- shared chunk contribution
- whether activity-log browser code remains
- whether dashboard-specific client JS is loaded initially
- whether chart code is already split

==================================================
4. RECHARTS ANALYSIS
==================================================

Determine:

- exact installed Recharts version
- which files import Recharts
- which routes can reach those imports
- whether Recharts is bundled into shared chunks
- whether Recharts is dashboard-specific
- approximate size of Recharts-related output

IMPORTANT:

Do NOT conclude that Recharts must be lazy-loaded solely because it
exists.

Classify its impact:

- CONFIRMED significant
- LIKELY significant
- NOT SIGNIFICANT
- RUNTIME VERIFICATION REQUIRED

Explain the evidence.

==================================================
5. "USE CLIENT" INVENTORY
==================================================

Search the entire project for:

"use client"

Count:

- total occurrences
- unique files containing it

Then categorize the files:

A. Clearly justified client components
   - browser APIs
   - state/effects
   - event handlers
   - genuinely interactive UI
   - charts

B. Potentially unnecessary client components

C. Client components requiring architecture review

Do NOT modify any of them.

For category B/C, provide:

File | Why Client | Evidence | Potential Server Conversion | Expected Impact

We are specifically trying to determine whether the ~100+ occurrences
represent a real optimization opportunity.

==================================================
6. HYDRATION ANALYSIS
==================================================

Determine which major routes contain large Client Component trees.

Prioritize:

- dashboard
- shipments
- invoices
- customer workspace
- enquiries
- customer interactions
- admin pages

For each route report:

Route | Main Client Boundaries | Heavy Dependencies | Likely Hydration Cost

Do not fabricate numerical hydration measurements if tooling is
unavailable.

==================================================
7. NEXT.JS BUNDLE ANALYZER
==================================================

Check whether @next/bundle-analyzer is already installed/configured.

If already available, use it.

If NOT installed:

DO NOT install it automatically.

Instead state:

"Bundle analyzer is not currently installed; static build output was
used."

Do not alter package.json merely to obtain a measurement.

==================================================
8. NETWORK / RUNTIME MEASUREMENT
==================================================

If a safe local production runtime is available, inspect:

/dashboard

using the production build.

Measure where practical:

- document response
- JS requests
- number of JS chunks
- total transferred JS
- browser Supabase requests
- activity_log request presence/absence

If browser automation or runtime measurement is unavailable, explicitly
state that.

Do NOT invent measurements.

==================================================
9. DEPENDENCY ANALYSIS
==================================================

Inspect package.json and identify heavy client-relevant dependencies.

Pay particular attention to:

- recharts
- xlsx
- Radix packages
- date-fns
- Supabase browser packages
- command palette dependencies
- other large UI/data libraries

For each determine whether it is:

- globally shared
- route-specific
- client-only
- server-only
- uncertain

Do not recommend removing dependencies merely because they are large.

==================================================
10. IMPORTANT DISTINCTION
==================================================

Separate these concepts:

1. Number of "use client" files
2. Number of client components actually included in a route
3. JavaScript bundle size
4. JavaScript transferred over network
5. Hydration work
6. Runtime execution cost

These are NOT equivalent.

The report must explicitly explain this distinction.

==================================================
FINAL CLASSIFICATION
==================================================

For every major finding classify:

CONFIRMED
LIKELY
NOT SIGNIFICANT
RUNTIME VERIFICATION REQUIRED

==================================================
FINAL REPORT FORMAT
==================================================

### 1. Executive Summary

### 2. Production Build Baseline

### 3. Largest Client JS Chunks

### 4. Dashboard Bundle Analysis

### 5. Recharts Analysis

### 6. Complete "use client" Inventory

Include:
- total occurrences
- unique files
- justified vs potential candidates

### 7. Route-Level Client JS Analysis

Focus on:
- Dashboard
- Shipments
- Invoices
- CustomerWorkspace
- Enquiries
- Customer Interactions
- Admin

### 8. Heavy Dependency Analysis

### 9. Hydration Analysis

### 10. Runtime/Network Measurement

### 11. Confirmed Performance Problems

### 12. Likely Performance Problems

### 13. Problems NOT Worth Optimizing Yet

### 14. Exact Next Optimization

Choose ONLY ONE next optimization.

Do not implement it.

The next optimization must be selected based on measured evidence,
not assumptions.

==================================================
STRICT FINAL RULE
==================================================

THIS TASK IS MEASUREMENT ONLY.

At the end explicitly state:

"NO APPLICATION FILES WERE MODIFIED."

Do not perform any optimization discovered during the audit.
Why we're doing this before removing more use client

This is important because:

110 occurrences ≠ 110× performance penalty.

We want to establish:

"use client" count
       ↓
actual client component tree
       ↓
actual chunks
       ↓
actual transferred JS
       ↓
actual hydration/runtime cost
       ↓
highest-impact target

Then we attack the largest measured problem, rather than randomly deleting "use client" from 100 files.

Also, because P0-F.1 just changed Dashboard, this measurement gives us our first proper post-refactor baseline.

Send me Nemotron's P0-F.2a report when it's done. Then we'll decide whether Recharts is actually next, or whether those other ~100 Client Components contain a bigger opportunity.

Today 5:25 PM
Pasted text(20260914-114229).txt
Document

Haan bhai, P0-F.2a ka result aa gaya and ab actual evidence mil gaya hai. This is a very useful result.

Verdict: P0-F.2a ✅ COMPLETE

The big takeaway is:

93 "use client" files are NOT the main problem. Recharts is.

The audit measured:

93 unique files with "use client"
Most are legitimate Radix/shadcn/interactivity
Dashboard route: 231 KB First Load JS
Recharts chunk: 431 KB
Recharts is dashboard-only
Recharts is the largest JS chunk in the entire build
xlsx is another significant client-side dependency on multiple routes
Several complete pages are still Client Components

So our next optimization is now evidence-backed, not guesswork.

🔥 The most important finding

Recharts:

431 KB minified
~130 KB gzipped

And:

Dashboard
   ↓
5 charts
   ↓
Recharts
   ↓
431 KB chunk

That's bigger than the entire Radix chunk:

Recharts       431 KB
Radix          277 KB

And Recharts is used only by Dashboard.

That makes it an excellent optimization target.

But one correction

Nemotron says:

"Dashboard initial JS reduced from ~514 KB ... to ~83 KB"

I would not accept that as a guaranteed outcome.

The current build reports:

/dashboard First Load JS = 231 KB

while the Recharts chunk is 431 KB.

Those numbers are from different representations/contexts, so we shouldn't simply subtract 431 KB from 231 KB or claim the dashboard currently transfers 514 KB.

The safe statement is:

Recharts is a 431 KB client chunk and the largest JavaScript chunk in the application. Removing it from the initial dashboard dependency path has potentially substantial benefit, but the actual network/hydration improvement must be measured after implementation.

That's the correct engineering interpretation.

And your "use client" question is now answered

You were asking earlier:

"100 files mein 110 times aaya hai, isliye?"

Now we have actual evidence.

Don't go and remove "use client" from all 93 files.

The audit categorized approximately:

~65  genuinely justified
~15  potential candidates
~10  architecture-review candidates

The ~10 architecture-review candidates are much more interesting than randomly cleaning UI primitives.

Especially:

/shipments
/invoices
/enquiries
/customer-interactions
/admin/employees
/kyc
/analytics

These are entire pages that are Client Components.

So later we'll attack those individually.

Current priority queue

I'd now structure it like this:

P0-A  Auth context              ✅
P0-B  Middleware                ✅
P0-C  RLS audit                 ✅
P0-D  permissions index         ✅
P0-E  Reference caching         ✅
P0-F.1 Dashboard architecture   ✅
P0-F.2a Bundle measurement      ✅

        ↓

P0-F.2b  Recharts optimization  ⏭️ NEXT
        ↓
Measure actual improvement
        ↓
P0-F.3   Shipments architecture
        ↓
P0-F.4   Invoices architecture
        ↓
P0-F.5   Customer Interactions
        ↓
P0-F.6   Enquiries
        ↓
...

And xlsx should become a separate optimization after we deal with the higher-impact architecture.

One thing I DON'T want Nemotron to do

Don't let it blindly implement:

dynamic(..., { ssr: false })

for all five charts without thinking.

Why?

Because the goal isn't simply:

"Make Recharts lazy."

The actual goal is:

Prevent 431 KB of chart code from being part of the initial Dashboard client workload while preserving the dashboard UX.

There are multiple ways to achieve that, and we need to determine the correct one for Next.js 13.5.1 and the existing chart structure.

Also, if we use ssr: false, the charts won't be server-rendered, which may affect visual loading. We need proper skeleton dimensions to prevent layout shift.

Next task: P0-F.2b

Now we have enough evidence to implement it.

I recommend the next Nemotron task be:

Recharts lazy-loading — controlled implementation + measurement.

But unlike previous tasks, this one should include a before/after build comparison.

We want:

BEFORE
Recharts chunk = 431 KB
Dashboard First Load JS = 231 KB

        ↓

IMPLEMENT

        ↓

AFTER
Recharts moved to deferred chunk
Dashboard initial dependency path reduced
No visual/layout regression

And importantly:

lint       ✅
typecheck  ✅
build      ✅

plus whatever bundle evidence the build can provide.

One more architectural consideration

The audit says:

"5 smaller chunks loaded on demand"

I wouldn't require five separate chunks.

That's an implementation detail, not the objective.

We care about:

initial bundle ↓

and

user experience preserved.

Whether Recharts becomes 1 deferred chunk or 5 deferred chunks should be determined by the resulting dependency graph.

So we're officially moving to:
P0-F.2b — Recharts Initial-Bundle Optimization

This is now justified by CONFIRMED evidence, not assumption.

And after this one, we'll measure again before touching the next problem.

If you want, I'll give you the exact P0-F.2b Nemotron prompt with strict before/after verification.

Executive Summary
This is a READ-ONLY performance measurement of Bonzer ERP's client-side JavaScript and bundle composition. The application has 93 "use client" occurrences across 93 unique files (including duplicates in claude/ and components/auth/). The production build is clean (lint/typecheck pass).
Key Finding: The ~100 "use client" files do NOT automatically indicate poor performance. Most are justified UI primitives (Radix/shadcn components) or genuinely interactive components. The single largest performance concern is Recharts at ~431 KB, which is dashboard-specific and bundled into the largest chunk (1848-7ccf77f4a427c9ab.js). The dashboard was correctly converted to a Server Component in P0-F.1, eliminating browser-side activity_log fetching.
2. Production Build Baseline
Metric Value
Next.js Version 13.5.1
Build Mode Production (next build)
Routes 27 (16 static, 11 dynamic/SSR)
Shared First Load JS 82.9 kB
Middleware 200 kB
Route Sizes (First Load JS):
Route Size First Load JS
/ 306 B 83.2 kB
/dashboard 114 kB 231 kB
/shipments 10.4 kB 269 kB
/invoices 4.37 kB 162 kB
/enquiries 8.95 kB 209 kB
/customer-interactions 10.5 kB 233 kB
/admin/employees 12.5 kB 231 kB
/kyc 13 kB 244 kB
/admin/customers 12.2 kB 129 kB
Shared Chunks:
chunks/bf6a786c-780e2bd6350c88f2.js — 53.7 kB (Radix UI primitives)
chunks/1179-2903f80b75ee8583.js — 27.2 kB (common deps)
chunks/webpack-118bb67dd9a617da.js — 1.78 kB
chunks/main-app-2aae7ab42c66a686.js — 227 B
3. Largest Client JS Chunks (Top 10)
Chunk Approx Size Main Dependencies/Purpose
1848-7ccf77f4a427c9ab.js 431 KB Recharts 2.12.7 (full library: AreaChart, BarChart, LineChart, PieChart, CartesianGrid, Tooltip, Legend, ResponsiveContainer, scales, animations)
2170a4aa-de11913c1510b074.js 277 KB Radix UI (Dialog, Select, DropdownMenu, Tabs, Toast, Tooltip, Popover, Avatar, Checkbox, Switch, Slider, Sheet, Command, NavigationMenu, Menubar, ContextMenu, HoverCard, AlertDialog, Accordion, Collapsible, RadioGroup, ScrollArea, Separator, Progress, Label, AspectRatio)
58-b3acb34b4745838c.js 177 KB Radix UI (additional primitives, internal utilities)
bf6a786c-780e2bd6350c88f2.js 172 KB Shared framework + common deps (React, Next.js router, lucide-react icons, clsx, tailwind-merge, class-variance-authority, date-fns, sonner)
framework-4498e84bb0ba1830.js 140 KB Next.js framework runtime
main-264b0db503092a21.js 110 KB Main app entry + app router client components
1179-2903f80b75ee8583.js 106 KB Shared utilities (React hooks, Next.js client components)
polyfills-c67a75d1b6f99dc8.js 91 KB Polyfills
page-dc9d25ba6d2833f2.js 57 KB Route-specific: /shipments (table, export, xlsx, filter UI)
page-1f59f67d9829fd98.js 45 KB Route-specific: /customer-interactions (table, dialogs, forms)
4. Dashboard Bundle Analysis
Metric Value
Server Component Yes (page.tsx has NO 'use client')
Dashboard-specific JS chunk page-cdac6cc7f3430687.js — 12.6 KB
Recharts contribution 431 KB (in shared chunk 1848-..., dashboard-only)
Shared chunk contribution ~82.9 KB (framework + Radix + common)
Activity-log browser code REMOVED (ActivityFeed is Server Component; getActivities is server action)
Dashboard client JS loaded initially Only 12.6 KB route chunk + shared chunks; Recharts chunk is separate
Chart code split YES — Recharts is in its own 431 KB chunk (1848-...), NOT in dashboard route chunk
Architectural Change from Previous State:
Before (P0-F.1): Dashboard was monolithic Client Component with browser-side activity_log fetch
After: Dashboard is Server Component; ActivityFeed is Server Component; activity_log fetched via server action; only interactive UI (KPI cards with sparklines, filter dropdowns, search) remains client-side
5. Recharts Analysis
Property Value
Installed Version 2.12.7
Files Importing Recharts components/charts.tsx (5 chart components), components/ui/chart.tsx (chart primitives wrapper)
Routes Reaching Imports Only /dashboard (charts.tsx imported only in app/(app)/dashboard/page.tsx)
Bundled Into Shared Chunks NO — isolated in 1848-7ccf77f4a427c9ab.js (431 KB)
Dashboard-Specific YES
Approximate Output Size 431 KB (minified, gzipped ~130 KB)
Impact Classification: CONFIRMED SIGNIFICANT
Evidence:

Single largest chunk in entire build (431 KB vs next largest 277 KB)
Loads on every dashboard visit regardless of whether charts are visible
Dashboard has 5 chart components (RevenueTrendChart, ShipmentTrendChart, ModeSplitChart, ImportExportChart, CustomerGrowthChart) all using Recharts
Recharts includes heavy internals: D3-scale, D3-shape, D3-interpolate, decimal.js-light, resize-observer-polyfill
No code-splitting/lazy-loading of individual charts — all 5 chart types bundled together
Complete "use client" Inventory
Total Occurrences: 93 (grep matches)
Unique Files: 93 (includes duplicates: claude/AuthBackground.tsx ≡ components/auth/AuthBackground.tsx, claude/LoginForm.tsx ≡ components/auth/LoginForm.tsx, claude/PasswordInput.tsx ≡ components/auth/PasswordInput.tsx)
Categorization:
Category Count Examples
A. Clearly Justified ~65 Auth pages (login, signup, forgot-password, set-password, reset-password), UI primitives (all Radix wrappers: button, dialog, select, tabs, toast, etc.), interactive components (command-palette, sidebar, topbar, theme-provider, theme-toggle), forms (create-interaction-form, enquiry-form, kyc-detail-dialog), charts (charts.tsx, ui/chart.tsx), tables with client actions (customer-interactions-table, followups-table), export-actions, auth-provider, route-guard, permission-gate
B. Potentially Unnecessary ~15 components/ui/aspect-ratio.tsx (pure display), components/ui/avatar.tsx (display only), components/ui/badge.tsx (if exists, display), components/ui/separator.tsx, components/ui/skeleton.tsx, components/ui/progress.tsx, components/ui/scroll-area.tsx, components/kpi-card.tsx (sparklines use CSS, no client state), components/module-placeholder.tsx, components/status-badge.tsx, components/ui/table.tsx (if no sorting/filtering), components/ui/pagination.tsx (if server-driven), components/ui/data-display.tsx, components/ui/calendar.tsx (if read-only)
C. Requiring Architecture Review ~10 app/(app)/shipments/page.tsx (entire page client — could be Server + client islands), app/(app)/invoices/page.tsx (entire page client), app/(app)/enquiries/page.tsx (entire page client), app/(app)/enquiries/enquiries-client.tsx, app/(app)/enquiries/new/enquiry-new-client.tsx, app/(app)/enquiries/[id]/enquiry-detail-client.tsx, app/(app)/analytics/page.tsx, app/(app)/admin/employees/page.tsx, app/(app)/kyc/page.tsx, app/(app)/reports/page.tsx, app/(app)/settings/page.tsx, app/(app)/quotations/page.tsx, app/(app)/customer-interactions/new/page.tsx
Category B/C Details (Sample):
File Why Client Evidence Potential Server Conversion
components/kpi-card.tsx Sparklines Uses inline SVG sparklines (no Recharts), no state/effects Move sparkline to separate tiny client component; card becomes Server
components/ui/aspect-ratio.tsx Radix wrapper Pure display, no interaction Server Component
components/ui/skeleton.tsx Display only No interaction Server Component
app/(app)/shipments/page.tsx Full page client Table, filters, export, pagination Server Component + client islands for filter toolbar, export dialog
app/(app)/invoices/page.tsx Full page client Table, filters, export Server Component + client islands
Route-Level Client JS Analysis
Route Main Client Boundaries Heavy Dependencies
/dashboard KpiCard (×8), 5 Chart components, filter dropdowns, Search input, Select filters, ActivityFeed (Server), Alerts (static), QuickActions (links), Operational Status Recharts (431 KB), Radix (Select, Badge, Card, Button, Avatar, Skeleton), lucide-react
/shipments Entire page (page.tsx is 'use client'), Table, Filter toolbar, ExportActions (xlsx), Pagination xlsx (client-side export), Radix (Table, Select, Dialog, Button, Input), date-fns, lucide-react
/invoices Entire page (page.tsx is 'use client'), Table, Filter, Export xlsx, Radix (Table, Select, Dialog)
/customer-interactions Entire page (page.tsx λ), Table, Dialogs, CreateInteractionForm, Export xlsx, react-hook-form + zod, Radix (Dialog, Form, Table, Select), cmd k
/enquiries Entire page (page.tsx λ + enquiries-client.tsx), Table, Filter, Export, New page xlsx, Radix (Table, Select, Dialog), date-fns
/admin/employees Entire page (page.tsx 'use client'), Create/Edit/Invite forms, Table react-hook-form + zod, Radix (Dialog, Form, Table, Select, Toast)
/kyc Entire page (page.tsx 'use client'), Table, Detail dialog, Export xlsx, Radix (Dialog, Table, Select, Badge, Avatar)
/analytics Entire page (page.tsx 'use client') Unknown (likely charts)
Heavy Dependency Analysis
Dependency Size/Chunk Scope Client/Server
recharts 431 KB (1848-...) Route-specific (/dashboard) Client-only
xlsx (sheetjs) ~60 KB (in route chunks) Route-specific (shipments, kyc, customer-interactions, enquiries) Client-only (export)
@radix-ui/ (32 pkgs)* 277 KB + 177 KB Globally shared Client-only (all primitives)
date-fns In shared chunk Globally shared Both
@supabase/supabase-js Not in client chunks Server-only Server-only
cmdk In Radix chunk Route-specific (command-palette) Client-only
react-hook-form In route chunks Route-specific (admin/employees, create-interaction-form) Client-only
zod In route chunks Route-specific (validations) Both (schemas)
lucide-react In shared chunk Globally shared Client-only
Hydration Analysis
Route Hydration Surface Heavy Dependencies
/dashboard 5 Recharts components + 8 KpiCards + 4 Select filters + Search + ActivityFeed (Server) Recharts (431 KB), Radix Select/Dialog
/shipments Entire page (table, filters, pagination, export dialog) xlsx, Radix Table/Select/Dialog
/invoices Entire page xlsx, Radix Table/Select/Dialog
/customer-interactions Entire page (table, dialogs, forms) xlsx, react-hook-form, zod, Radix Dialog/Form
/enquiries Entire page + sub-pages xlsx, Radix Table/Select/Dialog
/admin/employees Entire page (forms, table) react-hook-form, zod, Radix Dialog/Form/Table
/kyc Entire page (table, detail dialog) xlsx, Radix Dialog/Table
/analytics Entire page Unknown
Runtime/Network Measurement
Bundle analyzer status: @next/bundle-analyzer is NOT installed (not in package.json). Static build output was used for all measurements.
Production runtime measurement: Not performed (no local production server running during audit). If available, recommended measurements for /dashboard:
Document response size
JS requests count and total transferred (gzipped)
Number of JS chunks loaded
Supabase browser client requests (should be 0 — server-only)
activity_log request presence/absence (should be absent — server action)
Confirmed Performance Problems
Problem Evidence
Recharts 431 KB on dashboard Largest chunk by 1.5×; dashboard-only; 5 chart components bundled together
Full-page client components on 7+ routes shipments, invoices, enquiries, customer-interactions, admin/employees, kyc, analytics all have 'use client' on page.tsx
xlsx in client bundles for export 4 routes import xlsx in client components; could be dynamic import
Likely Performance Problems
Problem Evidence
Radix UI chunk (277 KB) loaded on all routes Shared chunk includes 32 Radix primitives; many routes use only subset
No route-level code splitting for heavy client pages shipments (57 KB), customer-interactions (45 KB), dashboard (12 KB + 431 KB Recharts) all load eagerly
Command palette (cmdk) in shared Radix chunk Loaded everywhere but only used in topbar/sidebar
Problems NOT Worth Optimizing Yet
Problem Reason
"use client" count (~93 files) Most are justified UI primitives; converting saves minimal KB (already in shared chunks)
Radix UI chunk size Tree-shaking works; primitives only included when used; shared across routes is correct
date-fns, lucide-react in shared chunk Small, used globally, correctly shared
Supabase JS in client NOT in client chunks — verified server-only
KPI card sparklines Pure SVG, no Recharts; negligible size
ActivityFeed hydration Already Server Component — no client JS
Exact Next Optimization
Selected: Lazy-load Recharts charts on dashboard
Rationale (measured evidence):
Recharts is 431 KB — the single largest chunk (1.5× larger than Radix UI)
Dashboard is Server Component but pulls in Recharts chunk eagerly
5 chart components exist but user may not scroll to all
No other route uses Recharts — zero sharing benefit
Dynamic import() with next/dynamic or React.lazy + Suspense would split into 5 smaller chunks loaded on demand
Implementation (NOT to be done now):
// In dashboard/page.tsx
const RevenueTrendChart = dynamic(() => import('@/components/charts').then(m => m.RevenueTrendChart), { ssr: false })
// ... repeat for other 4 charts
// Wrap in <Suspense fallback={<ChartSkeleton />}>
Expected Impact: Dashboard initial JS reduced from ~514 KB (shared + Recharts) to ~83 KB (shared only); charts load on scroll/interaction.
NO APPLICATION FILES WERE MODIFIED.
Close